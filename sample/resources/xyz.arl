archetype xyz(n : nat)

variable x : int = 0

entry assign (i : nat) {
  n := i
}

entry multi (a : nat, b : string, c : int) {
  n := a + length(b) + abs(c)
}

entry inc () {
  n += 1
}

entry f (str : string) {
  fail(str)
}

entry r (ra : rational) {
  fail((ra, "ceci est un message d'erreur"))
}

entry s (ra : rational) {
  x := 3
}
